{
  "name": "@coolwallets/wallet",
  "version": "0.0.10",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": " rm -rf node_modules && npm install && babel --presets es2015 -d lib/ src",
    "publish-single": "npm run build && npm publish"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@coolwallets/core": "0.0.10",
    "elliptic": "^6.5.1"
  },
  "devDependencies": {
    "@babel/core": "^7.7.2",
    "@babel/preset-env": "^7.7.1",
    "babel-loader": "^8.0.6",
    "babel-preset-es2015": "^6.24.1"
  }
}
